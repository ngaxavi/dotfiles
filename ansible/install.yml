# ansible-playbook -i "local," install.yml
---
 - name: Installation Software
   hosts: localhost
   connection: local
   remote_user: {{username}}
   roles:
     - yaourt
     - common
     - tmux
     - aur
     - base16
     - npm
     - fish


...
